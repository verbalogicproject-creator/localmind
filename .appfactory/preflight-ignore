# <check-id> | <reason>
#
# TEMPORARY. Delete the moment app/schemas/4.json is committed.
#
# The recurring deadlock: check 090 wants a committed schema for every declared
# @Database version, KSP generates it during the build, and preflight gates the push
# that triggers the build. No Android SDK here, so it cannot be made locally.
#
# Out: suppress, push, let CI generate and upload, commit it, delete this file.
090-db-version-has-schema | schema 4.json is generated by CI; committed in the follow-up, then this file is deleted
