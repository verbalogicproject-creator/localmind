# <check-id> | <reason>
#
# TEMPORARY. Remove the moment app/schemas/2.json is committed.
#
# The deadlock this resolves: check 090 requires a committed schema for every
# declared @Database version, but the schema JSON is generated by KSP during the
# build, and preflight gates the push that triggers that build. There is no Android
# SDK on this device, so it cannot be generated locally either.
#
# The sequence out is: suppress, push, let CI generate and upload the schema,
# commit it, delete this file. The migration test cannot run until that is done --
# MigrationTestHelper reads the exported schema from the test APK's assets.
090-db-version-has-schema | schema 2.json is generated by CI; committed in the follow-up, then this file is deleted
