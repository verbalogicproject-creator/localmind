# <check-id> | <reason>
#
# TEMPORARY. Delete this file the moment app/schemas/3.json is committed.
#
# Same deadlock as v2: check 090 requires a committed schema for every declared
# @Database version, the schema JSON is generated by KSP during the build, and
# preflight gates the push that triggers that build. No Android SDK on this device,
# so it cannot be produced locally either.
#
# Out: suppress, push, let CI generate and upload it, commit it, delete this file.
# MigrationTest cannot run until then -- MigrationTestHelper reads the exported
# schema from the test APK's assets.
090-db-version-has-schema | schema 3.json is generated by CI; committed in the follow-up, then this file is deleted
