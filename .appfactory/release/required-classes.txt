# Classes that must survive R8, beyond the ones named in AndroidManifest.xml.
#
# One class per line. verify_mapping.py asserts each is PRESENT in mapping.txt --
# a keep rule cannot preserve a class R8 deleted for being unreachable.
#
# ── com.arm.aichat.internal.InferenceEngineImpl ────────────────────────────────
#
# NOT LISTED YET, DELIBERATELY, and this note is the reminder to add it.
#
# It was listed, and CI refused it:
#
#     com.arm.aichat.internal.InferenceEngineImpl: REQUIRED but absent from the
#     release build -- R8 shrank it away as unused, so any claim about minifying
#     it is vacuous
#
# Which is correct. The vendored engine bindings are currently dead code: nothing
# in the app calls them until EmbeddedProvider exists, so R8 removes them, and an
# assertion that they survived minification would be asserting something false. A
# guard written before the thing it guards is a guard that reports on nothing.
#
# ADD THE LINE IN THE SAME COMMIT THAT MAKES THE ENGINE LIVE. From that point it is
# load-bearing: JNI resolves native methods by symbol name, libai-chat.so exports
# Java_com_arm_aichat_internal_InferenceEngineImpl_*, and a rename or removal by R8
# becomes an UnsatisfiedLinkError in the RELEASE build only -- invisible to every
# cheap rung, because debug builds are unminified.
#
#     com.arm.aichat.internal.InferenceEngineImpl
