# Classes that must survive R8, beyond the ones named in AndroidManifest.xml.
#
# One class per line. verify_mapping.py asserts each is PRESENT in mapping.txt --
# a keep rule cannot preserve a class R8 deleted for being unreachable.
#
# ── com.arm.aichat.internal.InferenceEngineImpl ────────────────────────────────
#
# Listed as of the commit that made the engine live. It was listed once before that,
# and CI was right to refuse it:
#
#     com.arm.aichat.internal.InferenceEngineImpl: REQUIRED but absent from the
#     release build -- R8 shrank it away as unused, so any claim about minifying
#     it is vacuous
#
# Nothing called the vendored bindings then, so R8 removed them and the assertion
# was asserting something false. EmbeddedEngine now calls them, so the class is
# reachable and the assertion means what it says.
#
# It is load-bearing: JNI resolves native methods by symbol name, libai-chat.so
# exports Java_com_arm_aichat_internal_InferenceEngineImpl_*, and a rename or removal
# by R8 becomes an UnsatisfiedLinkError in the RELEASE build only -- invisible to
# every cheap rung, because debug builds are unminified.
#
com.arm.aichat.internal.InferenceEngineImpl
