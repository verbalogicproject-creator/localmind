name: ci
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: bash scripts/preflight.sh
      - run: python3 scripts/verify_mapping.py mapping.txt
      - run: ./gradlew assembleDebug
