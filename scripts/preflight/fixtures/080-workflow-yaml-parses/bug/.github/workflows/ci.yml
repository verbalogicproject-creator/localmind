name: ci
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Do a thing
        run: |
          echo start
          RESULT=$(python3 - <<'PY'
print("flush-left heredoc terminates the block scalar")
PY
)
          echo "$RESULT"
